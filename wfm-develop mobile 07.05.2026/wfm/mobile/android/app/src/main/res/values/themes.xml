<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Основная тема приложения -->
    <style name="Theme.WFMApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Тема для Launch Screen (системный splash) -->
    <style name="Theme.WFMApp.Splash" parent="Theme.WFMApp">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
