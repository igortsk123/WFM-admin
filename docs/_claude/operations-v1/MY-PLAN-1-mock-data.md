# MY-PLAN-1: Mock data overhaul

**Размер:** большой (отдельный сеанс).
**Зависимости:** нет, базовый pass.
**Затрагивает:** все экраны (через mock-data, никаких UI-правок здесь).

## Задачи

### 1.1. Магазины — расширить до 12-15 с реальными адресами
**Файл:** `lib/mock-data/stores.ts`

Что есть сейчас (8 магазинов):
- Spar Томск: пр. Ленина 80, ул. Красноармейская 99, пр. Фрунзе 92а
- Spar Новосибирск: ул. Ленина 55, Красный пр. 200
- Spar Кемерово: пр. Советский 50
- Food City Томск: Global Market пр. Ленина 217, ул. Учебная 39

Добавить:
- **Spar в Северске** — найти 1-2 адреса в Северске (Дубль-ГИС: spar Северск)
- **Abricos** — определить город. User упомянул "Усть-Сити" — возможно это сетка магазинов "Усть-Сити" в Кемерово или Новосибирске. Если не найду — добавить как Abricos в Томске 1-2 адреса.
- **Первоцвет** — 1-2 магазина в Томске, реальные адреса.
- **Левушка** (новый партнёр fashion) — 1-2 магазина одежды в Томске
- **Техпроиздрав** (производство) — 1 швейный цех в Томске

Итого ~13-15 магазинов с realistic названиями.

### 1.2. Сотрудники — сгенерировать 100+ человек
**Файл:** `lib/mock-data/users.ts`

Сейчас ~30 в моках. Добавить до 100+.

Подход:
- Реалистичные ФИО (мужские/женские, разные)
- Распределить по магазинам пропорционально (например, на каждый магазин 5-10 сотрудников)
- Должности: универсал, кассир, старший кассир, продавец-консультант, кладовщик, мерчендайзер, директор, супервайзер, грузчик
- Зоны (после rename Привилегии→Зоны): торговый зал, склад, касса, прикассовая, холодильники
- Телефоны +7 формат, разные операторы
- avatar_url часть с null, часть с placeholder

**Подход к генерации:** написать helper `lib/mock-data/_generate-users.ts` с функцией которая берёт массив имён + распределяет по магазинам. Список ФИО взять из стандартного русского списка (Иванов, Петров и т.д.) + женские.

### 1.3. Зоны — взять из Memory Bank
**Файл:** `lib/mock-data/zones.ts`

Прочитать `C:/Users/SPECTRE/WFM/.memory_bank/` — найти определения зон для FMCG-ритейла. Заменить mock-зоны на конкретные из MB.

Если в MB нет — оставить текущий набор: торговый зал, склад, касса, прикассовая, самокассы, холодильники.

### 1.4. Партнёры — концепция
**Файл:** `lib/mock-data/partners.ts` (новый) ИЛИ расширить `organizations.ts`

Структура:
```ts
export interface Partner {
  id: string;
  name: string;
  business_vertical: 'FMCG_RETAIL' | 'FASHION_RETAIL' | 'PRODUCTION';
  legal_entity_ids: number[]; // несколько юрлиц
  store_brand_ids: string[]; // несколько брендов магазинов
  cities: string[];
}

export const MOCK_PARTNERS: Partner[] = [
  {
    id: 'lama',
    name: 'Lama',
    business_vertical: 'FMCG_RETAIL',
    legal_entity_ids: [1, 2], // ООО Спар Сибирь, ООО Спар Новосибирск (если уже есть)
    store_brand_ids: ['spar', 'abricos', 'pervotsvet'],
    cities: ['Томск', 'Новосибирск', 'Северск', 'Кемерово'],
  },
  {
    id: 'levushka',
    name: 'Левушка',
    business_vertical: 'FASHION_RETAIL',
    legal_entity_ids: [3],
    store_brand_ids: ['levushka'],
    cities: ['Томск'],
  },
  {
    id: 'tehproizdrav',
    name: 'Техпроиздрав',
    business_vertical: 'PRODUCTION',
    legal_entity_ids: [4],
    store_brand_ids: ['techproduzdrav'],
    cities: ['Томск'],
  },
];
```

### 1.5. Категории товаров — перепроверить с MB
**Файл:** `lib/mock-data/product-categories.ts`

Прочитать в Memory Bank какие категории определены. Заменить нереалистичные (если есть). User говорил их 53 шт.

### 1.6. Партнёр-фильтр в TopBar (структура)
**Файл:** `components/shared/admin-topbar.tsx` или новый компонент `partner-switcher.tsx`

Сейчас нет UI partner-switcher. План: НЕ делать UI на этой итерации (отдельный V0 чат). Только подготовить:
- В `lib/contexts/auth-context.tsx` — добавить `currentPartnerId: string` в AuthUser (default 'lama')
- Хелпер `usePartner()` → возвращает текущий partner
- Подготовка чтобы UI-фильтрация по партнёрам стала возможной

## Verification

После выполнения:
- `npx tsc --noEmit` zero errors
- Пройтись по экранам где списки магазинов/сотрудников — увидеть расширенные данные
- Не проверять Vercel деплой (лимит); локально на dev-server
