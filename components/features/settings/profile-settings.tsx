"use client";

/**
 * STUB — будет заменён V0 в чате 35 (profile settings).
 */
export function ProfileSettings() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">ProfileSettings stub — будет реализован V0 в chat 35.</p>
    </div>
  );
}
