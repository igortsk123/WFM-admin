"use client";

/**
 * STUB — будет заменён V0 в чате 55.
 */
export function ApplicationDetail({ id }: { id: string }) {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">ApplicationDetail stub (id={id}) — будет реализован V0 в chat 55.</p>
    </div>
  );
}
