"use client";

/**
 * STUB — будет заменён V0 в чате 60.
 */
export function AgentDetail({ id }: { id: string }) {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">AgentDetail stub (id={id}) — будет реализован V0 в chat 60.</p>
    </div>
  );
}
