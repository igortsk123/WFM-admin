"use client";

/**
 * STUB — будет заменён V0 в чате 38 (audit list).
 */
export function AuditList() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">AuditList stub — будет реализован V0 в chat 38.</p>
    </div>
  );
}
