"use client";

/**
 * STUB — будет заменён V0 в чате 28 (schedule calendar view).
 * Foundation от Claude содержит types/API/i18n/page-wrapper/nav-link.
 *
 * V0 заменит этот файл на реальный календарь (day/week/month view)
 * с использованием getSchedule, reopenShift, forceCloseShift из @/lib/api
 * и i18n namespace screen.schedule.*.
 */
export function ScheduleView() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">ScheduleView stub — будет реализован V0 в chat 28.</p>
    </div>
  );
}
