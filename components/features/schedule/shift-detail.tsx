"use client";

/**
 * STUB — будет заменён V0 в чате 29 (shift-detail).
 * Foundation от Claude содержит types/API/i18n/page-wrapper/nav-link.
 */
export function ShiftDetail({ shiftId }: { shiftId: number }) {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">ShiftDetail stub (shift_id={shiftId}) — будет реализован V0 в chat 29.</p>
    </div>
  );
}
