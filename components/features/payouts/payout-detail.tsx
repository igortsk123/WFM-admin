"use client";

/**
 * STUB — будет заменён V0 в чате 45 (payout detail).
 */
export function PayoutDetail({ id }: { id: string }) {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">PayoutDetail stub (id={id}) — будет реализован V0 в chat 45.</p>
    </div>
  );
}
