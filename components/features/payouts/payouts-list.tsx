"use client";

/**
 * STUB — будет заменён V0 в чате 45 (payouts list).
 */
export function PayoutsList() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">PayoutsList stub — будет реализован V0 в chat 45.</p>
    </div>
  );
}
