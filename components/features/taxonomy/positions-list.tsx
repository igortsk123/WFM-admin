"use client";

/**
 * STUB — будет заменён V0 в чате 32 (taxonomy positions list).
 */
export function PositionsList() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">PositionsList stub — будет реализован V0 в chat 32.</p>
    </div>
  );
}
