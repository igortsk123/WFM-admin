"use client";

/**
 * STUB — будет заменён V0 в чате 31 (taxonomy zones).
 * Foundation от Claude содержит types/API/i18n/page-wrapper/nav-link.
 */
export function ZonesList() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">ZonesList stub — будет реализован V0 в chat 31.</p>
    </div>
  );
}
