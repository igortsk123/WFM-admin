"use client";

/**
 * STUB — будет заменён V0 в чате 50 (risk scoring rules).
 */
export function RiskRules() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">RiskRules stub — будет реализован V0 в chat 50.</p>
    </div>
  );
}
