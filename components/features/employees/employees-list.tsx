"use client";

/**
 * STUB — будет заменён V0 в чате 22 (employees-list).
 * Foundation от Claude содержит types/API/i18n/page-wrapper.
 * V0 переопределяет содержимое этого файла полным компонентом списка.
 */
export function EmployeesList() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">EmployeesList stub — будет реализован V0 в chat 22.</p>
    </div>
  );
}
