"use client";

/**
 * STUB — будет заменён V0 в чате 24 (employee-create wizard).
 * Foundation от Claude содержит UserCreateData (с новыми полями permissions[],
 * invite_method, invite_message, notify_manager, rank, date_of_birth),
 * InviteMethod type, i18n screen.employeeCreate.*, page wrapper, nav-link.
 */
export function EmployeeCreateWizard() {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">EmployeeCreateWizard stub — будет реализован V0 в chat 24.</p>
    </div>
  );
}
