"use client";

/**
 * STUB — будет заменён V0 в чате 23 (employee-detail).
 * Foundation от Claude содержит types/API/i18n/page-wrapper/mock user 101.
 * V0 переопределяет содержимое этого файла полным компонентом карточки сотрудника.
 */
export function EmployeeDetail({ userId }: { userId: number }) {
  return (
    <div className="p-6 text-center text-muted-foreground">
      <p className="text-sm">EmployeeDetail stub (user_id={userId}) — будет реализован V0 в chat 23.</p>
    </div>
  );
}
