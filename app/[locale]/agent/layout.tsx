/**
 * STUB layout — будет заменён V0 в чате 61a (Agent cabinet).
 * V0 создаст полноценный layout с собственной sidebar для FREELANCE_AGENT.
 */
export default function AgentCabinetLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
